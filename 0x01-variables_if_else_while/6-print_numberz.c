#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Prints the lowercase alphabet in reverse.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i;

	for (i = 'z'; i >= 'a'; i--)
	{
		putchar(ch + '0');
	}
	putchar('\n');
	return (0);
}
